1. The discovery agent output is directly on the repo root. It should be align to the other agents output style.
2. When the dev container is build, it opens 2 ports automatically. Fix that
3. The user should be involve (via questions) in the initial phase until the implementation. This way, the user knows what’s going on exactly and the sure the app is based on his/her choice. The agents should ask as many questions as possible, unless the user says to skip questions or is not giving helpfull answers.Any step before the implementation should be a back and forth conversation between the agents and the user.
4. The plan agent prompt and template seems to be rewritten. Can you get the old one back (before the last commit and push)? I Just wanted the plan agent to ask questions about architecture, hosting,... instead of guessing.
5. Make sure the agents behave consistently.
6. The discovery agent should ask questions about the project and the user needs before starting the discovery process. This way, the discovery process is more focused and relevant to the user's requirements.